# CLI demo only needs httpx
httpx>=0.27

# Pipecat voice agent. The exact extras depend on your provider stack.
# Pin to a known-working version range; Pipecat changes frame APIs
# every few releases.
pipecat-ai[daily,deepgram,openai,cartesia,silero]>=0.0.49
