# CLI demo only needs httpx
httpx>=0.27

# Pipecat voice agent. This example uses Sarvam for STT/TTS/LLM.
# Pin to a known-working version range; Pipecat changes frame APIs
# every few releases.
pipecat-ai[daily,sarvam,silero]>=0.0.49
